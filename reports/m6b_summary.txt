ENSEMBLE
The three architectures agreed unanimously on 76.7% of test images, leaving 23.3% contested; on 89.1% of those contested images at least one member held the correct grade, which is the headroom an ensemble can recover. Averaging the members' softmax outputs (gmean) gave a test QWK of 0.8935 (95% CI [0.866, 0.919]), against 0.8910 ([0.865, 0.916]) for the best single model, densenet121_weighted.

A paired bootstrap of the difference gives -0.0025 (95% CI [-0.025, 0.021]), which includes zero, so the improvement is within sampling noise and the ensemble should be reported as no worse rather than as better. The ensemble costs three forward passes per image instead of one, so on this evidence it is justified by its calibration and stability rather than by a large accuracy gain.

CALIBRATION
Before calibration the model was overconfident on the test split: mean confidence 0.833 against an accuracy of 0.804, giving an expected calibration error of 0.0461. Temperature scaling with T = 1.258, fitted on the validation split only, raised ECE to 0.0536 and moved mean confidence to 0.799. Because temperature scaling divides the logits by a positive constant it cannot reorder them, so accuracy and QWK are unchanged at 0.8036; only the reported confidence changes. A paired bootstrap of the ECE change gives +0.0075 (95% CI [-0.0229, +0.0251]), which includes zero: on a split of this size ECE cannot resolve a change this small, so it is reported as unchanged rather than as an improvement. The binning-free scoring rules give NLL -0.0074 (better) and Brier +0.0003 (worse).